var maybe_window=guiMouseCommon(true);

if (maybe_window!=noone)
    {
    //dunno, maybe a context menu? it can be done as a borderless window!
    mouse_clear(mb_right);
    }
else
    {
    //unlock this mouse action for the pipeline
    
    }
